data_c0=[.4;.5;.6;.7];
data_A4=[.0713;.110;.152;.193];

plot(data_c0,data_A4);

a=10^6;
b=1;

c0=linspace(0.35,0.75,100);
c1_eq=zeros(1,100);
cfib=zeros(



lambda=a/b;
for i=1:100
    c1_eq(i)=2*c0(i)/(1+2*lambda*c0(i)+(1+4*lambda*c0(i))^0.5);
end
hold on
